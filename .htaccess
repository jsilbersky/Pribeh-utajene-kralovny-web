# Redirect explicit index.html requests to the canonical directory URL
RewriteEngine On
RewriteCond %{THE_REQUEST} /index\.html [NC]
RewriteRule ^index\.html$ / [R=301,L]

# Handle 404 errors with a simple custom page
ErrorDocument 404 /404.html
